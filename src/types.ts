export type Site = {
  TITLE: string;
  DESCRIPTION: string;
  AUTHOR: string;
};

export type Page = {
  TITLE: string;
  DESCRIPTION: string;
};

export type Links = {
  TEXT: string;
  HREF: string;
}[];

export type Socials = {
  NAME: string;
  ICON: string;
  TEXT: string;
  HREF: string;
}[];